language: node_js
cache:
  yarn: true
  directories:
    - node_modules
notifications:
  email: false
node_js:
  - '6'
  - '8'
script:
  - yarn run validate
after_success:
  - yarn run report-coverage
#- yarn run semantic-release
branches:
  only:
    - master
    - dev
