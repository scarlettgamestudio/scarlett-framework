/**
 * GameScene class
 */
var GameScene = (function() {

    /**
     * @constructor
     */
    function GameScene(params) {
        params = params || {};

        this.name = params.name || "GameScene";
    }

    return GameScene;

})();